<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cast_libraries_material_featurehighlight_text_header_color">


    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/CatSelect"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="14dp"
        tools:context="cs65.confuse.MapActivity" />

    <Button
        android:id="@+id/pet_button"
        style="@style/Widget.AppCompat.Button.Colored"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_below="@+id/map"
        android:background="@color/fullNameColor"
        android:clickable="true"
        android:focusable="true"
        android:text="@string/pet"
        android:visibility="visible" />

    <EditText
        android:id="@+id/CatSelect"
        style="@style/Widget.AppCompat.Light.AutoCompleteTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_toStartOf="@+id/pet_button"
        android:clickable="false"
        android:ems="10"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:inputType="textMultiLine"
        android:text="@string/hello_please_click_on_a_paw" />

    <ImageView
        android:id="@+id/catpic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_alignTop="@+id/CatSelect"
        android:layout_toStartOf="@+id/CatSelect"
        android:scaleType="fitCenter"
        android:visibility="visible"
        app:srcCompat="@drawable/clickicon" />

</RelativeLayout>